---
interface Props {
    title: string;
    more?: boolean
    english?: boolean
}

const { title, more, english } = Astro.props;
---

<section id={title.toLowerCase()}>
    <header class="flex justify-between items-center mb-8">
        <h2 class="text-3xl font-bold mb-4 text-primary">{title}</h2>
        {more ? 
        <a href={english ? "/en/projects" : "/projects"} class="group mb-2 inline-flex items-center font-semibold leading-tight hover:text-accent text-sm gap-1 text-secondary">
            {english ? "Check more" : "Ver más"}
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="mr-1 h-3 w-3 rotate-360 transition-transform group-hover:translate-x-2" aria-hidden="true">
                <path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd"></path>
            </svg>
        </a>
        : null
    }
    </header>
        <slot/>
</section>